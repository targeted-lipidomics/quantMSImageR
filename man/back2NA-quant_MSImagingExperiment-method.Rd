% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/back2NA.R
\name{back2NA,quant_MSImagingExperiment-method}
\alias{back2NA,quant_MSImagingExperiment-method}
\title{Set background pixel intensities to NA}
\usage{
\S4method{back2NA}{quant_MSImagingExperiment}(
  MSIobject,
  val_slot = "response",
  background = "Noise",
  tissue = "Tissue",
  sample_type = "sample_type",
  ...
)
}
\arguments{
\item{MSIobject}{A \code{quant_MSImagingExperiment} object whose \code{pData()} contains
a column identifying pixel type.}

\item{val_slot}{Character. Name of the spectra slot to modify (default \code{"intensity"}).}

\item{background}{Character. Value in \code{pData(MSIobject)[[sample_type]]} that
labels background/noise pixels (default \code{"Noise"}).}

\item{tissue}{Character. Value that labels tissue pixels (default \code{"Tissue"}).
Currently unused but kept for API symmetry with \code{int2snr}.}

\item{sample_type}{Character. Column name in \code{pData()} holding pixel-type
labels (default \code{"sample_type"}).}
}
\value{
The input \code{quant_MSImagingExperiment} with background pixels set to
\code{NA} in \code{val_slot}. Returns the object unchanged if no background pixels are
found.
}
\description{
Replaces the spectral values of background/noise pixels with \code{NA} in a
chosen intensity slot. Tissue pixels are left unchanged.
}
